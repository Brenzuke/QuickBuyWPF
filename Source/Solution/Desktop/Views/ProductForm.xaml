<UserControl x:Class="Desktop.Models.Forms.ProductForm"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Height="Auto" Width="450">

	<Grid>
		<!-- Rows -->
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

		<!-- Columns -->
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="Auto"/>
			<ColumnDefinition Width="*"/>
		</Grid.ColumnDefinitions>

		<!-- Name -->
		<Label Content="Nome: " Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>
		<TextBox x:Name="NameField" Grid.Row="0" Grid.Column="1" Margin="5" Padding="5"/>

		<!-- Price -->
		<Label Content="Preço: " Grid.Row="1" Grid.Column="0" VerticalAlignment="Center"/>
		<TextBox x:Name="PriceField" Grid.Row="1" Grid.Column="1" Margin="5" Padding="5"/>

		<!-- Quantity -->
		<Label Content="Quantidade: " Grid.Row="2" Grid.Column="0" VerticalAlignment="Center"/>
		<TextBox x:Name="QuantityField" Grid.Row="2" Grid.Column="1" Margin="5" Padding="5"/>

		<!-- Add to cart -->
		<Button Content="Adicionar ao Carrinho" Grid.Row="4" Grid.Column="1" 
				Margin="5" Padding="6" Click="AddProductToCart" VerticalAlignment="Center"/>
	</Grid>
</UserControl>